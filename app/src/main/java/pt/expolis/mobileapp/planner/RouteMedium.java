package pt.expolis.mobileapp.planner;

/**
 * An enumeration that represents different route mediums.  Each one corresponds to a set of OSRM
 * servers.  The other route parameter is to avoid pollution or not.
 */
enum RouteMedium
{
	FOOT,
	BICYCLE,
	CAR,
}
